# -------------------------------
# 🔧 CONFIGURACIÓN GENERAL
# -------------------------------
spring.application.name=login-service
server.port=8081

# -------------------------------
# 🔐 CONFIGURACIÓN JWT
# -------------------------------
jwt.secret=${JWT_SECRET:mySecretKeyForJWTTokenGenerationThatIsLongEnough123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Configuración de Refresh Token (7 días)
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION:604800000}


cors.allowed-origins=*

# -------------------------------
# 💾 CONEXIÓN A BASE DE DATOS NEON
# -------------------------------
# Variables de entorno con valores por defecto para desarrollo local
# En producción, configurar las variables: DATABASE_URL, DATABASE_USERNAME, DATABASE_PASSWORD
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-super-unit-ahnue215-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_B9eNc4LGsxwm}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# -------------------------------
# 📚 CONFIGURACIÓN SPRINGDOC OPENAPI
# -------------------------------
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
